import React from 'react';

const ArticleCard = ({ article }) => {
  return (
    <div className="article-card">
      {/* Article card implementation will go here */}
    </div>
  );
};

export default ArticleCard; 