import React from 'react';

const CategoryFilter = ({ onFilterChange }) => {
  return (
    <div className="category-filter">
      {/* Category filter implementation will go here */}
    </div>
  );
};

export default CategoryFilter; 